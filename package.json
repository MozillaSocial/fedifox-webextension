{
  "scripts": {
    "start": "cd src && web-ext run",
    "dev": "cd src && web-ext run --devtools --browser-console",
    "lint": "cd src && web-ext lint"
  },
  "dependencies": {
    "web-ext": "^7.11.0"
  }
}
